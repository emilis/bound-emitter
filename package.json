{
  "name": "bound-emitter",
  "version": "1.0.0",
  "description": "A function to get an EventEmitter with bound methods for a given event list.",
  "main": "bound-emitter.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "EventEmitter",
    "bound",
    "events",
    "emitter"
  ],
  "author": "Emilis Dambauskas <emilis.d@gmail.com>",
  "license": "GPL-3.0",
  "devDependencies": {
    "browserify": "^11.0.0",
    "uglify": "^0.1.5"
  }
}
